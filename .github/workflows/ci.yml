name: Code quality

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  static-analysis:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install quality tools
        run: python -m pip install --upgrade pip ruff

      - name: Check syntax and critical errors
        run: ruff check src data --select E9,F63,F7,F82

      - name: Compile Python sources
        run: python -m compileall -q src data
